name: react-pipeline
'on':
  push:
    branches:
      - main
    paths:
      - '**/reactdocker/**'
env:
  DOCKER_BUILD_FILE_PATH: ./reactdocker/DockerFile.build
  DOCKER_FILE_PATH: ./reactdocker/DockerFile.publish
  DOCKER_BUILD_CONTEXT: reactdocker
  DOCKER_PUBLISH_CONTEXT: ./build
  CONTAINER_NAME: react-pipeline-test
  REPOSITORY_NAME: react-pipeline-test
jobs:
  build_and_deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Check Out Repo
        uses: actions/checkout@v3
      - name: Login to Azure Container Registry
        uses: azure/docker-login@v1
        with:
          login-server: ${{ secrets.CR_LOGIN_SERVER }}
          username: ${{ secrets.CR_USERNAME }}
          password: ${{ secrets.CR_PASSWORD }}
      - name: Build and Test
        run: docker build ${{ env.DOCKER_BUILD_CONTEXT }} -f ${{ env.DOCKER_BUILD_FILE_PATH }} -t ${{ secrets.CR_LOGIN_SERVER }}/${{ env.REPOSITORY_NAME }}:react-pipeline-test
      - name: Runs docker
        run: docker run --name ${{ env.CONTAINER_NAME }} -d ${{ secrets.CR_LOGIN_SERVER }}/${{ env.REPOSITORY_NAME }}:react-pipeline-test
      - name: copy build files
        run: |-
          docker cp ${{ env.CONTAINER_NAME }}:/app/build/ ./
          docker rm ${{ env.CONTAINER_NAME }}
      - name: Build and  push image to container registry
        run: |-
          docker build ${{ env.DOCKER_PUBLISH_CONTEXT }} -f ${{ env.DOCKER_FILE_PATH }} -t ${{ secrets.CR_LOGIN_SERVER }}/${{ env.REPOSITORY_NAME }}:react-pipeline-test
          docker push ${{ secrets.CR_LOGIN_SERVER }}/${{ env.REPOSITORY_NAME }}:react-pipeline-test
